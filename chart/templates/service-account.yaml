{{- if .Values.aws-iam-role-arm -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  annotations:
    eks.amazonaws.com/role-arn: {{ .Values.aws-iam-role-arn }}
  labels:
    app: {{ template "elasticsearch.name" . }} #TODO: Change
    chart: {{ .Chart.Name }}-{{ .Chart.Version }}
    heritage: {{ .Release.Service }}
    release: {{ .Release.Name }}
  name: {{ template "elasticsearch.fullname" . }} #TODO: Change